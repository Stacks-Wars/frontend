"use client"

/**
 * Side-effect imports that populate the game registry.
 *
 * Adding a game to the platform UI means dropping a folder under `games/` and
 * adding one line here — nothing else in the app needs to change.
 */
import "@/games/checkers"
import "@/games/lexi-wars"
import "@/games/ludo"
import "@/games/ludo-rush"
